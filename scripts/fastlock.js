var password = prompt("Enter password", "");
if (password != "Dupa"){
    var canvas = $('#canvas_id')[0];
    var buttonCanvas = $('#buttons')[0];
    canvas.style.display = "none";
    buttonCanvas.style.display = "none";
};